# DTx Backend — Python dependencies
# Install: pip install -r requirements.txt

fastapi>=0.136.0
uvicorn[standard]>=0.49.0
pydantic>=2.12.0
httpx>=0.28.0          # used by FastAPI TestClient
apscheduler>=3.10.0    # background scheduler (AsyncIOScheduler)
tzlocal>=5.0           # APScheduler timezone support

# ── PostgreSQL / SQLAlchemy (v0.7.0 — DB Migration) ──────────────────────────
sqlalchemy>=2.0.0      # ORM + Core; supports both Postgres and SQLite
psycopg2-binary>=2.9   # PostgreSQL DBAPI driver (binary wheel — no pg_config needed)
alembic>=1.13.0        # Schema migrations (autogenerate from ORM models)

# ── LLM Router (AI Mentor Coach) ─────────────────────────────────────────────
anthropic>=0.40.0      # Anthropic Claude SDK — model: claude-sonnet-4-6
python-dotenv>=1.0.0   # Load ANTHROPIC_API_KEY from .env (NEVER hardcode)

# ── Production ASGI Server ────────────────────────────────────────────────────
gunicorn>=21.2.0       # production process manager; spawns uvicorn worker processes

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0          # test runner
